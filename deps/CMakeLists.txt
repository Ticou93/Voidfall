PROJECT(deps)
CMAKE_MINIMUM_REQUIRED(VERSION 3.2)

SET(OpenGL_GL_PREFERENCE GLVND)

ADD_LIBRARY(deps INTERFACE)

#ADD_SUBDIRECTORY(glew)
#
#ADD_LIBRARY(glew INTERFACE)
#TARGET_INCLUDE_DIRECTORIES(deps INTERFACE glew/include)
#SET_TARGET_PROPERTIES(glew PROPERTIES LINKER_LANGUAGE CXX)
#TARGET_LINK_LIBRARIES(glew INTERFACE glew)

# GLFW
# If we are running Windows with Visual Studio compile to Multi-Threaded instead of DLL.
#if (MSVC)
#	SET(CMAKE_C_FLAGS_DEBUG "/MTd")
#	SET(CMAKE_C_FLAGS_RELEASE "/MT")
#	SET(CMAKE_C_FLAGS_MINSIZEREL "/MT")
#	SET(CMAKE_C_FLAGS_RELWITHDEBINFO "/MT")
#endif()
ADD_SUBDIRECTORY(glfw)

ADD_SUBDIRECTORY(bgfx)

TARGET_LINK_LIBRARIES(deps INTERFACE glfw bgfx)
